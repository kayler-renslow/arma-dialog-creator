ADDED:
    none
CHANGED:
    for submit button for input fields, toggle color of button to show different states (data is submitted, not submitted)
    input field will now no longer submit data after focus lost unless there is no text entered and field doesn't allow empty data
FIXED:
    changing ui scale manipulates safeZone controls when it shouldn't
