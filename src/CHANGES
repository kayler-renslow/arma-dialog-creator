ADDED:
    trivial changelog implementation for future use
CHANGED:
    some work on the project opening
    instead of application properties being saved when application closes, it saves when the choose directories popup closes
    doesn't save/create project folder until the project itself is saved
FIXED:
    none
