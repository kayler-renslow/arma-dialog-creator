ADDED:
    none
CHANGED:
    made selection border for canvas editor less opaque
    how application properties are saved, loaded, and accessed (now using xml file format loaded into DataContext instead of Properties instance)
FIXED:
    off by a pixel for renderer (changed rounding method for PositionCalculator)
